apiVersion: v2
name: transaction-outbox
description: Transactional Outbox pattern for a Payments domain — ingestion-api plus one consumer-worker per payment method, autoscaled via KEDA.
type: application
version: 0.1.0
appVersion: "1.0.0"
