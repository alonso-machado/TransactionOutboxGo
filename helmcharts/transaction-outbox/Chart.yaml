apiVersion: v2
name: transaction-outbox
description: Transactional Outbox pattern for an Event Ticket System — ingestion-api plus one order-consumer-worker and one fulfillment-consumer-worker per event-type shard, autoscaled via KEDA.
type: application
version: 0.3.0
