# Mounted into the rabbitmq container at /etc/rabbitmq/conf.d/ — without
# return_per_object_metrics, the rabbitmq_prometheus plugin only exposes
# cluster-wide aggregates, not per-queue depth/consumer/ack metrics, which
# the infra and consumer-worker Grafana dashboards both rely on.
prometheus.return_per_object_metrics = true
