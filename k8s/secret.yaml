# PLACEHOLDER SECRET — do NOT use these values in any real environment.
# Real DATABASE_URL / RABBITMQ_URL credentials are injected by CI/CD
# (e.g. GitHub Actions secrets, HashiCorp Vault, or Sealed Secrets) at
# deploy time. Never commit actual passwords or connection strings here.
apiVersion: v1
kind: Secret
metadata:
  name: transaction-outbox-secrets
  namespace: transaction-outbox
type: Opaque
data:
  # base64("postgres://CHANGE_ME:CHANGE_ME@postgres:5432/outbox?sslmode=disable") — placeholder only
  DATABASE_URL: Q0hBTkdFX01F
  # base64("amqp://CHANGE_ME:CHANGE_ME@rabbitmq:5672/") — placeholder only
  RABBITMQ_URL: Q0hBTkdFX01F
